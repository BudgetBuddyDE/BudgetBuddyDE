import type {ColorSystemOptions} from '@mui/material/styles';
import {colors} from './colors';

export const ColorOptions: ColorSystemOptions = {
  opacity: undefined,
  overlays: undefined,
  palette: colors,
};
