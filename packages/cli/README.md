# @budgetbuddyde/cli

The BudgetBuddyDE command-line interface.

## Usage

```sh
npm run build
./bin/run.js
./bin/run.js status
```

The banner is rendered by the root command only. Commands such as `status` do
not render it.
