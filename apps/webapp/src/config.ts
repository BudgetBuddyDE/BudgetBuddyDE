export const config = {
  tables: {
    /**
     * Number of items to show per page in paginated lists. This setting is used across the application to ensure consistency in pagination behavior.
     */
    itemPerPage: 10,
  },
};
