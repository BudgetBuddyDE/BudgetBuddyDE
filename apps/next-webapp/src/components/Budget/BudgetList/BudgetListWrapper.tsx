'use client';

import { BudgetList } from './BudgetList';

export const BudgetListWrapper = () => {
  return (
    <BudgetList
      // FIXME: Uncomment and implement handlers when ready
      // onAddBudget={handler.onAddBudget}
      // onEditBudget={handler.onEditBudget}
      // onDeleteBudget={handler.onDeleteBudget}
      // onClickBudget={handler.onClickBudget}
      onAddBudget={() => {}}
      onEditBudget={() => {}}
      onDeleteBudget={() => {}}
      onClickBudget={() => {}}
    />
  );
};
